{
  "name": "weight-program-schema",
  "version": "1.0.0",
  "description": "An open-source JSON schema for defining weightlifting programs",
  "scripts": {
    "test": "mocha",
    "clean": "find src -type f ! -name typings.d.ts \\( -name *.js -o -name *.d.ts \\) -delete",
    "prepublishOnly": "npm run build",
    "build": "tsc"
  },
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "author": "Colby M. White",
  "license": "ISC",
  "dependencies": {
    "jsonschema": "^1.1.1"
  },
  "devDependencies": {
    "@types/chai": "^4.0.1",
    "@types/mocha": "^2.2.41",
    "@types/node": "^8.0.10",
    "chai": "^4.0.2",
    "mocha": "^3.4.2",
    "ts-node": "^3.2.0",
    "typescript": "^2.4.1"
  }
}
